version: '3.8'

services:
  math-pwa:
    image: httpd:2.4-alpine
    container_name: math_table_pwa
    ports:
      - "8080:80"
    volumes:
      - .:/usr/local/apache2/htdocs/
    restart: unless-stopped
